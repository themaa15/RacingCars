DROP TABLE Car;

DROP TABLE Car IF EXISTS;

INT GENERATED BY DEFAULT AS IDENTITY
    (START WITH 10 INCREMENT BY 10),

--Derby-: Achtung auf mit "," vor INCREMENT
CREATE TABLE CAR
(
id INTEGER NOT NULL GENERATED ALWAYS AS IDENTITY 
            (START WITH 1, INCREMENT BY 1),
carname VARCHAR(255),
driver VARCHAR(255),
time_ DOUBLE,
place INTEGER,
PRIMARY KEY(ID)
);

